<!-- header -->
		<header class="header-dark">
		  <div class="container">
		    <nav class="navbar navbar-expand-lg navbar-dark">
		      <a class="navbar-brand" href="{{ path('home') }}">
		        <div class="font-weight-bold grey__dark">SnowTricks</div>
		      </a>
		      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="burger"><span></span></span></button>

		      <div class="collapse navbar-collapse" id="navbarSupportedContent">
		        <ul class="navbar-nav align-items-center mr-auto">
		          <li class="nav-item">
		            <a class="nav-link font-weight-bold grey__dark" href="{{ path('home') }}" role="button">
		              Accueil
		            </a>
		          </li>
		          <li class="nav-item">
		            <a class="nav-link font-weight-bold grey__dark"  href="{{ path('allTricks') }}" role="button">
		              Tricks
		            </a>
		          </li>
				  {% if app.user is not null %}
					<li class="nav-item">
						<a class="nav-link font-weight-bold grey__dark"  href="{{ path('profile', {'id': app.user.id}) }}" role="button">
							Profile
						</a>
					</li>
				  {% endif %}
		        </ul>

		        <ul class="navbar-nav align-items-center mr-0">
		          <li class="nav-item ">
					{% if app.user is null %}
						<a class="nav-link font-weight-bold grey__dark" href="{{ path('login') }}">
							Connexion
						</a>
					{% else %}
						<a class="nav-link font-weight-bold grey__dark" href="{{ path('logout') }}">
							Déconnexion
						</a>
					{% endif %}
		          </li>
		        </ul>
		      </div>
		    </nav>
		  </div>
		</header>
		<!-- header -->
